import { type WebDriver } from 'selenium-webdriver';
import { WebdriverConfigParams } from '../types';
declare const startDriver: (config: WebdriverConfigParams) => Promise<WebDriver>;
export { startDriver };
